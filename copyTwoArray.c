#include<stdio.h>
int main(){

    //make first array
      int m;
      scanf("%d",&m);
     int arr[m];
      for(int i=0; i<m; i++){
        scanf("%d",&arr[i]);
      }

      //make second array
      int n;
      scanf("%d",&n);
     int arr[n];
      for(int i=0; i<n; i++){
        scanf("%d",&arr[i]);
      }
      int combArrLen=m+n;
      int copyArr[combArrLen];
      for(int i=0; i<combArrLen;i++){
         
      }

    return 0;
}